# Node / Tailwind
node_modules/
npm-debug.log
yarn-error.log
package-lock.json

# Editores / SO
.DS_Store
Thumbs.db
.vscode/
.idea/

# Sensibles
data/users.json
data/_inbox/
data/_rate_limit*

# Imágenes subidas por el cliente: las guardamos en producción,
# no se versionan (se respaldan por FTP).
uploads/*
!uploads/.gitkeep
!uploads/projects/.gitkeep
!uploads/services/.gitkeep
!uploads/hero/.gitkeep

# Salida de PHP / logs
error_log
*.log
